# LookML Source Files

@../CLAUDE.md

LookML files mirroring the Looker repo directory structure. To be promoted to `SoundstripeEngineering/looker` via the Looker IDE.

## Structure
- `views/` — `.view.lkml` files; target path in Looker repo: `views/Finance/`
- `explores/` — explore fragments to append to a model file (`Models/General.model.lkml` or `Models/Finance.model.lkml`)

See parent `CLAUDE.md` for the file inventory and promotion path.
