# QA Queries — MQL Discrepancy Fix

@../CLAUDE.md

## Purpose

Post-merge QA queries run against `soundstripe_prod` to validate the MQL pipeline fix.

## Table References

- `soundstripe_prod.TRANSFORMATIONS.fct_sessions_build`
- `soundstripe_prod.CORE.fct_sessions`
- `soundstripe_prod.MARKETING.dim_mql_mapping`
- `soundstripe_prod.MARKETING.dim_session_mqls`
- `soundstripe_prod.hubspot.hubspot_forms`
- `soundstripe_prod.staging.stg_contacts_2`
