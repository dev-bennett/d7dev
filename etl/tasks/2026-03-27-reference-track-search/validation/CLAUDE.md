# Validation Queries -- Reference Track Search

@../CLAUDE.md

## Table References

- dbt-produced tables: `core.fct_events` (default connector uses soundstripe_prod; prefix with database name only when targeting a specific environment like soundstripe_dev for QA)
- Query results exported as CSV files named to match query labels (a.csv, b.csv, etc.)
