# Project 07: Agent cost dispatch

@../CLAUDE.md

## Project purpose
Annotate agents with `expected_context_cost` and make the orchestrator cost-aware so sub-agents are only dispatched when the cost of primary-context handling exceeds the delegation cost.

## Linked framework section
§4.2 (Agents)
