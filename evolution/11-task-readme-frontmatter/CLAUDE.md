# Project 11: Task README frontmatter

@../CLAUDE.md

## Project purpose
Every task directory gets a README with strict frontmatter; abandoned is a first-class status; `/orient` and `/status` read from frontmatter instead of string-parsing.

## Linked framework section
§6.2 (Per-task contract)
