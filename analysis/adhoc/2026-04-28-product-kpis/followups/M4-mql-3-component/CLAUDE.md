# M4 — MQL 3-Component Decomposition

@../CLAUDE.md

Decomposes tile 6 (MQLs / session) into its three constituent form-submission counts and quantifies the upper bound on visitor double-counting in the LookML 3-component sum.

Source data: `../../q01.csv` (visitors_mql_pricing, _enterprise, _demo already pulled separately). No new SQL required.
