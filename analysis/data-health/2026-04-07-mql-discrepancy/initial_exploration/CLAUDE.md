# Initial Exploration

@../CLAUDE.md

Starting context queries pulled from Looker explores. Three queries:
1. HubSpot MQL count (enterprise form submissions where `became_mql IS NOT NULL`)
2. Mixpanel MQL count from `fct_sessions` (enterprise MQL columns)
3. MQL mapping query joining HubSpot forms to Mixpanel sessions by timestamp/URL/VID proximity
